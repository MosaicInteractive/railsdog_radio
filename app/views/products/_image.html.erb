<% image ||= @product.images.first %>
<% if image %>
  <div id="main-image">
    <%= link_to image_tag(image.attachment.url(:product)), image.attachment.url(:original), :rel => "facebox" %>
  </div>
  Click image to enlarge
<% end %>

